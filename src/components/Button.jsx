// Start coding here

function Button(props) {
  return (
    <button
      style={{
        background: props.backgroundColor,
      }}
    >
      Button
    </button>
  );
}

export default Button;
